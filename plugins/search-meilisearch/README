# Meilisearch 搜索插件

该插件使用 Meilisearch 为 Outline 提供 CJK 全文搜索，不需要修改
PostgreSQL 扩展、镜像或 schema。文档和集合的权限仍由 Outline 与
PostgreSQL 复核，Meilisearch 仅保存搜索索引。

## 配置

在 Outline 服务的环境变量中设置：

```dotenv
SEARCH_PROVIDER=meilisearch
MEILISEARCH_URL=http://meilisearch:7700
MEILISEARCH_API_KEY=<服务端专用密钥>
MEILISEARCH_INDEX_PREFIX=outline
```

`MEILISEARCH_URL` 与 `MEILISEARCH_API_KEY` 必填；未同时设置时插件不会
注册。`MEILISEARCH_INDEX_PREFIX` 可选，默认值为 `outline`。同一个
Meilisearch 实例服务多个环境时，应为每个环境使用不同前缀，例如
`outline-prod` 与 `outline-staging`。

`MEILISEARCH_API_KEY` 只能配置在服务端，不要暴露给浏览器。开发或小型
自托管部署可以使用 Meilisearch 的 `MEILI_MASTER_KEY`；生产环境建议创建
仅供此服务使用的 API key。

## 可选的语义增强搜索

语义搜索默认关闭。开启后，插件仍以 CJK 关键词搜索为主，同时使用自建的
OpenAI 兼容 Embeddings 服务补充语义相近的文档；它不调用生成式 LLM，也不
生成回答。

```dotenv
MEILISEARCH_SEMANTIC_SEARCH_ENABLED=true
MEILISEARCH_EMBEDDING_URL=http://infinity:7997
MEILISEARCH_EMBEDDING_API_KEY=<Embedding 服务密钥>
MEILISEARCH_EMBEDDING_MODEL=BAAI/bge-m3
MEILISEARCH_EMBEDDING_DIMENSIONS=1024
MEILISEARCH_EMBEDDING_CONCURRENCY=1
MEILISEARCH_DOCUMENT_CHUNK_MAX_PAYLOAD_BYTES=5242880
```

`MEILISEARCH_EMBEDDING_URL` 必须是服务基址，不要包含 `/v1` 或
`/embeddings`；插件会固定请求 `POST /embeddings`。启用语义功能时，上述
Embedding 配置必须完整；
`bge-m3` 的 dense 向量默认是 1024 维，若实际服务使用其他维度，必须同步
设置 `MEILISEARCH_EMBEDDING_DIMENSIONS`。

`MEILISEARCH_EMBEDDING_CONCURRENCY` 只控制全量 `search:reindex` 时的并行
Embedding 请求数，默认值为 `1`。CPU 推理建议保持 `1`，避免请求排队超过
Node 的网络超时；仅在 GPU 服务已验证可稳定处理并发请求后再逐步调高。

`MEILISEARCH_DOCUMENT_CHUNK_MAX_PAYLOAD_BYTES` 控制单次向 Meilisearch 写入
文档 chunk 的最大 JSON 请求体大小，默认值为 5 MiB（`5242880`）。插件会按
实际字节大小自动拆分请求，以低于 Meilisearch 默认的 100 MB 限制；通常无需
修改。

Meilisearch 必须使用支持 vector search 的 `v1.x.y` 版本。插件会为
`document_chunks` 索引配置 `userProvided` embedder：Infinity 只负责编码，
Meilisearch 保存向量并完成向量检索，因此不需要 pgvector、Qdrant 或 Milvus。

文档正文会按约 1,000 个字符切片（相邻片段重叠约 150 个字符），并将标题
附入每个片段的 Embedding 输入。标题搜索和集合搜索仍是原有的 CJK 关键词
搜索。文档更新会异步重建该文档的向量；移动、归档和成员权限变化只更新索引
元数据，不重复编码正文。

全文搜索会并行查询关键词索引与向量索引，再使用偏向关键词结果的 RRF 排名
融合。精确标题、编号、URL 和 CJK 关键词命中优先，向量结果仅补充措辞不同但
语义相近的文档。两路候选都会经过 Meilisearch 过滤，并始终由 Outline 和
PostgreSQL 再次复核权限。

Embedding 服务暂时不可用时，文档索引任务会失败并由 Outline 队列重试；已有
的 CJK 索引不受影响。查询时会自动退化为纯关键词搜索，并记录服务端错误。

Meilisearch 服务需要持久化其数据目录。Docker Compose 中的示例：

```yaml
meilisearch:
  image: getmeili/meilisearch:v1
  environment:
    MEILI_MASTER_KEY: <长随机密钥>
  volumes:
    - meilisearch_data:/meili_data
```

生产环境应固定实际使用的 `v1.x.y` 镜像版本，并将 Meilisearch 仅暴露给
Outline 服务所在的内部网络。

## 首次部署与重建索引

从源码运行 Outline 时，先编译插件，再建立现有文档的索引：

```bash
yarn build
yarn search:reindex
```

`yarn search:reindex` 会扫描 PostgreSQL 中已有的文档和集合，写入临时索引，
完成后原子交换到正式索引。它不是每次启动都会自动执行，避免每次部署都对
数据库和 Meilisearch 做一次完整扫描。

启用语义增强后，该命令还会建立临时的文档片段向量索引，并在文档、集合与
向量索引均完成后一次性交换。首次启用或更换 Embedding 模型、维度时都必须
重新执行该命令；如果编码或向量索引失败，正式索引不会被替换。

重建进度保存在 Redis，按 `MEILISEARCH_INDEX_PREFIX` 隔离。进程、容器或
Embedding 服务中断后，直接再次执行相同命令会从上次成功的 ID checkpoint
继续；文档 chunk 以单篇文档为单位提交 checkpoint，中断时最多重做当前或已
并发完成但尚未 checkpoint 的文档。恢复期间始终继续使用旧正式索引，只有
所有临时索引完成后才会 swap 发布。

查看未完成任务而不改动任何索引：

```bash
node build/plugins/search-meilisearch/server/reindex.js --status
```

需要明确废弃未完成任务并从头开始时，使用：

```bash
node build/plugins/search-meilisearch/server/reindex.js --fresh
```

`--fresh` 会删除该重建任务引用的临时索引与 Redis 进度。恢复时会校验是否启用
语义搜索、Embedding 模型、向量维度和 chunk 格式版本；这些配置发生变化时会
拒绝恢复并提示使用 `--fresh`，避免将不同向量空间的数据混入同一索引。Redis
被清空时无法找回临时任务进度，也应使用 `--fresh` 重新建立索引。

重建提供“原子发布、尽力收敛”而不是 PostgreSQL 严格快照：若容器中断后索引
锁过期，极少数文档变更可能先写入旧正式索引；后续索引事件或下一次全量重建会
收敛该窗口。Outline 的 PostgreSQL 授权复核始终保留，重建过程不会因此返回未
授权文档。

首次索引后，文档、集合及其成员权限变更会通过 Outline 队列自动更新索引。
仅在首次部署、索引损坏或需要全量修复时重新执行该命令。重建期间索引写入
会通过 Redis 锁串行化，避免并发更新被旧扫描结果覆盖。

## Docker 镜像部署

通过本 fork 的 Dockerfile 或 GitHub Actions 构建镜像时，`yarn build` 已在
镜像构建阶段执行，运行中的容器不需要再次执行它。首次部署后，在 Outline
容器内执行：

```bash
node build/plugins/search-meilisearch/server/reindex.js
```

使用 Docker Compose 时可写为：

```bash
docker compose exec outline node build/plugins/search-meilisearch/server/reindex.js
```

将 `outline` 替换为实际的 Compose 服务名。命令运行时必须带有与 Outline
服务相同的数据库、Redis 与 Meilisearch 环境变量。

## Fork 维护约定

- `main`：仅同步官方上游。
- `cjk/meilisearch`：持续开发分支，用于构建开发镜像。
- `release/vX.Y.Z-cjk`：从对应上游 tag 创建，再移植 CJK 补丁；用于稳定镜像
  `vX.Y.Z-cjk.N`。

GitHub Actions 工作流位于 `.github/workflows/build-cjk-image.yml`。它只从
`release/<upstream_tag>-cjk` 构建稳定镜像，并在构建前重新验证该 tag 来自
官方上游。

## 同步新的上游 tag

当上游发布新稳定 tag（以下以 `v1.10.0` 为例）时，先拉取上游 tag，并将
`main` 快进到上游开发分支：

```bash
git fetch upstream --tags
git switch main
git merge --ff-only upstream/main
git push origin main
```

然后将新的 `main` 合并到开发分支。若有冲突，在该分支解决、提交并推送：

```bash
git switch cjk/meilisearch
git merge main
git push origin cjk/meilisearch
```

新稳定发布分支应从上一个 CJK 发布分支创建，并将完整补丁栈重放到新 tag。以
当前 `v1.9.2` 发布分支为例：

```bash
git switch release/v1.9.2-cjk
git switch -c release/v1.10.0-cjk
git rebase --onto v1.10.0 v1.9.2
```

如果 rebase 冲突，解决文件后执行 `git add <文件>` 与 `git rebase --continue`。
完成静态检查后推送发布分支：

```bash
git push -u origin release/v1.10.0-cjk
```

最后手动运行 GitHub Actions，填写 `upstream_tag=v1.10.0` 与
`revision=cjk.1`。发布分支承载完整 CJK 补丁栈，因此会包含已同步到当前稳定
分支的所有 CJK 修复。后续新增 CJK 修复时，应同时提交到
`cjk/meilisearch` 与最新的 `release/*-cjk` 分支。
